name: Update Scholarships

on:
  schedule:
    # Runs every day at 03:00 UTC
    - cron: '0 3 * * *'

  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Scholarship Updater
        run: |
          python scripts/update_scholarships.py

      - name: Commit Changes
        run: |
          git config --global user.name "Scholarship Bot"
          git config --global user.email "actions@github.com"

          git add scholarships.json

          git diff --cached --quiet || git commit -m "🤖 Automatic scholarship update"

          git push
